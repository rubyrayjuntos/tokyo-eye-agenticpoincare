# Plan — tokyo_eye_equ_spread_hold

## Goal
Recover std(r_H) &gt; 0.15 on Equiformer-pool + geoopt without patching theme_restore Fail θ; biology AUPRC watch-only.

## Tasks
1. Pins: spine_init=geoopt_restore best; forbid theme_restore Fail; volume/spread coeffs bumped vs theme_restore.
2. `equ_spread_hold.py` + runner forking theme_restore / geoopt_restore (pool frontend + warm restore).
3. Seal on geometry HOLD only; log theme AUPRC as telemetry.
4. Execute in tokyoeye_science; MLflow `tokyo_eye_equ_spread_hold`.
