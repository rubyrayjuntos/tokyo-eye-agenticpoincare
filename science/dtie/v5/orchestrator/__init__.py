# V5 orchestrator — inherits from v4 orchestrator pattern
"""V5 DTIE orchestrator — same narrow focus as v4 but uses v5 GNN.

The v5 orchestrator is identical in scope to v4 (source-leak / allosteric)
but uses the decoupled radial-angular model for better differential analysis.
"""
