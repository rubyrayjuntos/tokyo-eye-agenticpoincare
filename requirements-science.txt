# Heavy science dependencies — pinned versions
# These run inside the Docker container only.
# Regenerate: uv pip compile pyproject.toml --extra science -o requirements-science.txt

# Shared (from [project.dependencies])
numpy==1.26.4
pydantic==2.8.2
psycopg[binary]==3.2.1
psycopg-pool==3.2.2
python-dotenv==1.0.1

# Science-specific (from [project.optional-dependencies.science])

# PyTorch ecosystem (CUDA 12.1)
torch==2.3.1
torch-geometric==2.5.3
# torch-scatter and torch-sparse installed separately in Dockerfile
# with --find-links for correct CUDA wheel matching

# Hyperbolic geometry
geoopt==0.5.0

# SE(3) equivariance
e3nn==0.5.1

# Topological data analysis (Phase 3)
gudhi==3.10.1

# Structural biology
biotite==1.0.1
biopython==1.84
freesasa>=2.2,<3

# Cheminformatics (Phase 5, 6)
rdkit==2024.03.5

# RCSB Data API client (installed from local copy — PyPI version requires Python >=3.11)
# rcsbapi is installed via COPY + pip install below
httpx==0.27.0

# Scientific computing
scipy==1.13.1
scikit-learn==1.5.1
scikit-learn-extra==0.3.0
hdbscan==0.8.40
openmm==8.5.2
pandas==2.2.2
networkx==3.3

# Transitive dependencies (pinned for reproducibility in GPU environment)
# NumPy/SciPy ecosystem
packaging==24.1
typing-extensions==4.12.2

# Data handling
pytz==2024.1

# Scientific computing
joblib==1.4.2

# V6 training lifecycle (MLflow tracking, optional Optuna sweeps)
mlflow==2.14.3
optuna==3.6.1
pyyaml==6.0.2
