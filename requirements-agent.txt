# Agent coordinator dependencies — lightweight, no ML
# Pinned lockfile for Docker builds.
# Regenerate: uv pip compile pyproject.toml --extra agent -o requirements-agent.txt

# Shared (from [project.dependencies])
numpy==1.26.4
pydantic==2.8.2
psycopg[binary]==3.2.1
psycopg-pool==3.2.2
python-dotenv==1.0.1

# Agent-specific (from [project.optional-dependencies.agent])
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
matplotlib==3.9.2

# AWS Bedrock (LLM provider)
boto3>=1.34.0
anthropic>=0.25.0

# Transitive dependencies (pinned for reproducibility)
# FastAPI ecosystem
starlette==0.39.0
pydantic-core==2.18.4
typing-extensions==4.12.2

# Uvicorn ecosystem
h11==0.14.0
httptools==0.6.1

# Cryptography
cffi==1.17.1
cryptography==42.0.8
